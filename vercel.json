{
  "version": 2,
  "name": "comic-api",
  "builds": [{ "src": "/server.ts", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "/server.ts" }],
  "functions": {
    "server.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
